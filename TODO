TODO:

* Allow user to enter in db connection info (host/username/password/etc)
* Add update/delete/create ability when looking at table rows
* Don't save bad queries
* Use <C-]> to move between foreign key fields
* Move in blocks of 5 up and down with CTRL + D and CTRL + U
* Add tabs for multiple queries
* Add ability to switch databases

